// Sprint 0 placeholder route. Real behavior (a quiet recovery path back to
// Home or Contact, per the Product Blueprint's Product Map, Section 1) is
// implemented in a later sprint.
export default function NotFound() {
  return <p>Not found — Sprint 0 placeholder.</p>;
}
