// Sprint 0 placeholder route. No design, no components, no real content —
// this file exists only to prove the route resolves. Built out in the
// sprint that implements the Home page per the Product Blueprint (Section 3).
export default function HomePage() {
  return <p>Home — Sprint 0 placeholder.</p>;
}
